{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "Restricted capability for the prewarmed overlay windows (search + generator). Only enough to show/hide/focus, listen+emit events, and write secrets to the clipboard. No clipboard read, no global-shortcut registration, no dialogs.",
  "windows": ["overlay-search", "overlay-generator"],
  "permissions": [
    "core:default",
    "core:window:allow-hide",
    "core:window:allow-show",
    "core:window:allow-set-focus",
    "core:window:allow-close",
    "core:event:allow-listen",
    "core:event:allow-emit",
    "clipboard-manager:allow-write-text"
  ]
}
